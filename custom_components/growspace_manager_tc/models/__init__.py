"""Domain models for Growspace Manager TC.

Nothing here imports Home Assistant.  The models validate and serialize
themselves so the repository can be exercised — and the rules argued with —
without a `hass`.
"""
